import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'XBank App',
      home: Scaffold(
        appBar: AppBar(
          title: Text('XBank App'),
        ),
        body: Center(
          child: Text('Welcome to XBank!'),
        ),
      ),
    );
  }
}
